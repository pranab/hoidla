Poor man's HDFS backed key value store

What it is
==========
- lightweight key value store supporting updatre and delete
- Backed by sequenece file
- Data is implicitly versioned since HDFS is not mutable
- Allows compaction to reduce fragmentation
- Supports the familiar abstraction of Database and table

